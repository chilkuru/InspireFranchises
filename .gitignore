target/
test-output/

# TestLink MCP — local secrets (each developer creates their own)
testlink/.env

# TestLink MCP server — installed dependencies (restore with `npm install`)
testlink-mcp/node_modules/
*.class
*.iml
.idea/
.DS_Store

# Office temp files (created while a file is open)
~$*

# Keep the Maven Wrapper JAR out of source control —
# mvnw / mvnw.cmd will download it automatically on first run.
.mvn/wrapper/maven-wrapper.jar
